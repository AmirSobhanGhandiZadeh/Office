package model;

public class CustomerBusiness extends Person {
    public CustomerBusiness(String firstName, String lastName, String nationId, String address) {
        super(firstName, lastName, nationId, address);
    }
}
