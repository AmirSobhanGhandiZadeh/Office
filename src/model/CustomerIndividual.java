package model;

public class CustomerIndividual extends Person {

    public CustomerIndividual(String firstName, String lastName, String nationId, String address) {
        super(firstName, lastName, nationId, address);
    }
}
